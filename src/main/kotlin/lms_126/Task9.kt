package lms_126

fun task9(): String {

    return """
        OUTPUT "select option"
        INPUT withdraw
        OUTPUT " enter amount to withdraw"
        INPUT amount
        OUTPUT "confirm the amount is correct"
        INPUT yes
        OUTPUT " transaction is in process"
        OUTPUT " take your money"
        OUTPUT " press Enter to end Or back to do another process"
        
        
    """.trimIndent()
}
